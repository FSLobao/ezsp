"""
python — Microsoft Graph / SharePoint client abstractions powered by MSAL.

Subpackages:
  auth        — MSAL authentication helpers (GraphAuthenticator)
  client      — Graph HTTP client (GraphClient, GraphAuthorizationError)
  drive       — Document library (drive) operations
  lists       — SharePoint list operations
"""
